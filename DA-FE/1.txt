server {
    server_name 3.95.239.19;

    # Enable Gzip compression
    gzip on;
    gzip_proxied any;
    gzip_types application/javascript application/x-javascript text/css text/javascript;
    gzip_comp_level 5;
    gzip_buffers 16 8k;
    gzip_min_length 256;

    sendfile on;
    tcp_nopush on;
    types_hash_max_size 2048;
    client_max_body_size 10M;

    # Serve static files for React app
    location / {
        alias /var/www/DA-FE/dist/;
        try_files $uri /index.html;
        expires 365d;
        access_log off;
    }

    # Proxy to React app on port 4173
    # location / {
        #proxy_pass http://127.0.0.1:4173;
        #proxy_http_version 1.1;
        #proxy_set_header Upgrade ;
        #proxy_set_header Connection 'upgrade';
        #proxy_set_header Host ;
        #proxy_cache_bypass ;
    #}

    # Proxy for the API on port 9000
    location /api/v1 {
        proxy_pass http://127.0.0.1:9000;
        #proxy_set_header Host ;
        #proxy_set_header X-Real-IP ;
        #proxy_set_header X-Forwarded-For ;
        #proxy_set_header X-Nginx-Proxy true;
    }

    listen 443 ssl; # managed by Certbot
    ssl_certificate /etc/letsencrypt/live/localhost/fullchain.pem; # managed by Certbot
    ssl_certificate_key /etc/letsencrypt/live/localhost/privkey.pem; # managed by Certbot
    include /etc/letsencrypt/options-ssl-nginx.conf; # managed by Certbot
    ssl_dhparam /etc/letsencrypt/ssl-dhparams.pem; # managed by Certbot
}



    CREATE USER 'root'@'localhost' IDENTIFIED WITH mysql_native_password BY 'keith0309';
GRANT ALL PRIVILEGES ON my_app.* TO 'root'@'localhost';


telnet da-instance.cz2muugsm551.us-east-1.rds.amazonaws.com 3306
